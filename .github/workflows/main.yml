name: Repository Dispatch Workflow

on:
  repository_dispatch:
    types: [echo_message]

jobs:
  echo_job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo message
        env:
          MESSAGE: ${{ github.event.client_payload.message }}
        run: echo "$MESSAGE"
